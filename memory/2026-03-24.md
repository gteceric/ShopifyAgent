# 2026-03-24

Daily workspace notes.

## Session Log

- Made `alreadyFullyRefunded` configurable in the refund policy so merchants can choose between automatic `ineligible` and `manual_review`.
- Kept the default behavior safe: fully refunded orders remain `ineligible` unless policy config opts into review.
- Added wrapper tests for `checkRefundEligibility`, switched the demo entrypoint to call the wrapper, and replaced the Shopify loader stub with a mock Shopify-shaped adapter that normalizes into `RefundPolicyInput`.

## Things Worth Promoting Later

- Decide which refund policy settings should become merchant-configurable in v1 beyond refunded-order handling.

## Scratch
