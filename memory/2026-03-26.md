# 2026-03-26

Daily workspace notes.

## Session Log

- Added a deterministic refund-response formatter that converts `agentQuestion + tool result` into agent-facing natural language.
- Wired the scenario matrix runner to print `formattedAgentResponse` alongside the structured policy output.
- Added formatter-focused tests covering eligible, ineligible, manual-review, and VIP-override response paths.
- Added `respondToRefundQuestion()` as an agent responder layer that wraps the refund tool, exposes an optional model-backed `generateResponse` hook, and falls back to deterministic wording when needed.
- Updated the demo entrypoint and scenario runner to use the responder layer, and added tests for responder success and fallback behavior.
- Added an OpenAI-backed `generateResponse` implementation using the Responses API via native `fetch`, plus tests that mock the API payload offline.
- Wired the demo entrypoint to attempt the OpenAI responder when `OPENAI_API_KEY` is present, while keeping the scenario matrix deterministic unless `USE_OPENAI_RESPONDER=true`.

## Things Worth Promoting Later

- Decide whether to keep the deterministic formatter as a baseline fallback once a real model-backed response layer is added.

## Scratch
